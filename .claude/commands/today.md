---
description: 오늘의 할일(PDCA) 파일을 다음 사이클로 갱신한다
---

`docs/TODAY.md` 를 다음 작업 사이클용으로 갱신해 주세요.

절차:

1. 현재 `docs/TODAY.md` 를 읽어 어디까지 됐는지 파악합니다.
2. `logs/worklog/` 의 최근 일지 1~2개를 읽어 막힌 지점을 확인합니다.
3. `docs/MASTER_PLAN.md` 의 Phase 로드맵에서 현재 Phase와 완료 조건을 확인합니다.
4. 아래 기준으로 새 `docs/TODAY.md` 를 씁니다.
   - **현재 Phase / 직전 완료 / 오늘의 목표 한 줄** 을 사실대로 갱신
   - **P(Plan)** 은 항목당 30분~1시간 크기로 쪼갤 것. **하루 5~7개를 넘기지 말 것**
   - 어제 못 끝낸 항목은 맨 위로 올리고, 왜 못 끝냈는지 한 줄 붙일 것
   - **"오늘 안 할 것"** 목록을 반드시 채울 것 (범위가 새는 걸 막는 칸입니다)
   - D / C / A 칸은 빈 양식으로 초기화
   - 맨 아래 접힌 블록에 다음 Phase 미리보기를 넣을 것
5. 현재 Phase의 완료 조건이 이미 충족됐다면, 다음 Phase로 넘어가자고 먼저 제안하세요.

계획이 현실과 어긋났으면 `docs/MASTER_PLAN.md` 쪽을 고쳐야 하는지도 알려주세요.

$ARGUMENTS
