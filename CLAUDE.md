# sale — 인스타툰 제휴수익 자동화

## 이 프로젝트가 뭔가

쿠팡파트너스 / 토스 쉐어링크 제휴 링크로 수익을 내는 인스타툰 계정의 제작 파이프라인.
키워드 → 후킹 기획 → 콘티(JSON) → 작화 → 말풍선 합성 → **사람 검수** → 인스타 발행.

## 항상 지킬 것

1. **계획서가 기준이다.** 방향이 헷갈리면 `docs/MASTER_PLAN.md`, 오늘 할 일은 `docs/TODAY.md`.
2. **오늘의 할일 범위를 벗어나지 않는다.** 벗어나야 하면 먼저 물어본다.
3. **손으로 1번 성공 → 스크립트화 → 자동화.** 순서를 뒤집지 않는다.
4. **완전자동 금지.** 발행 직전 사람 검수 단계는 끝까지 남긴다.
5. **비밀값은 `.env` 에만.** API 키·토큰을 코드·문서·커밋 메시지에 절대 쓰지 않는다.
6. **제휴 고지 문구는 필수.** 게시물 생성 코드에는 항상 `disclosure` 필드가 들어간다.
   표준 문구: `이 게시물은 쿠팡 파트너스 활동의 일환으로, 이에 따라 일정액의 수수료를 제공받습니다.`
7. **작업자는 초보자다.** 명령어는 복붙 가능한 형태로, 왜 그걸 치는지 한 줄 설명과 함께 준다.
   에러가 나면 원인을 먼저 설명하고 고친다.

## 기록 규칙

- 커밋하면 `logs/worklog/` 에 일지 뼈대가 **자동 생성**된다 → 네 칸을 실제 내용으로 채운다.
- 작업이 진행되면 `docs/TODAY.md` 의 체크박스와 D(Do) 칸을 그때그때 갱신한다.
- Phase가 끝나면 `/log` 로 마일스톤 일지를 남기고 `/today` 로 다음 사이클을 연다.
- 일지에 **안 된 것·막힌 것을 반드시 남긴다.** 성공만 적힌 일지는 쓸모가 없다.

## 명령어

| 명령 | 하는 일 |
|---|---|
| `/log` | 지금까지 한 작업을 마일스톤 작업일지로 기록 |
| `/today` | `docs/TODAY.md` 를 다음 사이클로 갱신 |

## 기술 스택

원안은 **단계별로 잘하는 LLM을 나눠 쓰는 분업 구조**다. 한 모델이 다 하지 않는다.

| 단계 | 원안 담당 | 지금 실제 |
|---|---|---|
| 트렌드 소싱 | 그록 (X 실시간) | 🔴 API 차단 → **폰 앱으로 수동** |
| 소재 앵글 | GPT | 🔴 API 차단 |
| **콘티 생성** | **Claude** (`claude-opus-5` → 안정되면 `claude-sonnet-5`) | ⚠️ **Gemini 로 임시 운영** |

> ⚠️ **콘티 담당이 문서(Claude)와 코드(Gemini)에서 다르다.** Phase 0 에서 확보된 키가
> Gemini 뿐이라 급히 갈아탄 것이고 최종 상태가 아니다. `api.anthropic.com` 은 도달하므로
> 앤트로픽 키만 넣으면 원복 가능 → **Phase 11**. 코드를 고칠 땐 이 임시성을 전제로 볼 것.

- 합성: Python + Pillow (한글 폰트는 상업적 이용 가능한 것만)
- 승인 알림: 텔레그램 봇 (**Phase 10**, 원안 항목) — 🔴 정책 차단
- 오케스트레이션: n8n (Docker, **Phase 6에서 도입** — 그전엔 파이썬 스크립트로 충분)
- 발행: Instagram Graph API — 🔴 정책 차단

## 네트워크 정책 (작업 전 반드시 인지)

이 컨테이너는 **정책 프록시** 뒤에 있다. 도달하는 건 **Anthropic·Google 둘뿐**이고
그록·GPT·쿠팡·인스타·텔레그램·슬랙은 전부 `403 policy denial` 로 막힌다.

- 외부 API 를 붙이는 작업 전에 **먼저 도달 여부를 확인**한다. 코드부터 짜지 않는다.
- 막혀 있으면 **우회하지 않는다.** TLS 검증을 끄거나 프록시를 벗어나려 하지 말 것.
  사용자에게 "환경 설정 → 네트워크 정책" 에서 열어야 한다고 보고한다.
- 진단: `curl -sS "$HTTPS_PROXY/__agentproxy/status"` → `recentRelayFailures` 확인.
- 상세 표는 `docs/MASTER_PLAN.md` 4-0절.

## 놓치면 안 되는 함정

- **인스타 캡션·댓글의 URL은 클릭이 안 된다.** 유일하게 클릭되는 건 프로필 링크(링크인바이오).
  Phase 5 전에 링크인바이오 페이지부터 만들어야 한다.
- **Graph API 요건**: 프로페셔널 계정 + Facebook 페이지 연결 + `instagram_content_publish` 권한 승인.
  하루 25건 제한, 캐러셀 최대 10장, 토큰 만료 → 갱신 로직 필요.
- **캐릭터 일관성**이 최대 기술 난이도. 레퍼런스는 `assets/character/` 에 고정.
- **대량 자동 게시는 스팸 판정 위험.** 주 3~5건이 안전선.
