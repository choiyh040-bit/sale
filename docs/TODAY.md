# 오늘의 할일 — 2026-08-31

> PDCA 한 사이클 = 하루. 이 파일은 **덮어쓰며 굴립니다.**
> 하루가 끝나면 `/log` 로 일지에 옮겨 담고, `/today` 로 이 파일을 다음 날 것으로 갱신합니다.

## 현재 위치

- **현재 Phase**: Phase 0·1·2 ✅ 완료 / **Phase 9 소재 큐 ✅ 완료 (2026-09-03)**
- **다음**: Phase 3 작화(사람) → Phase 4 원클릭 → Phase 11 Claude 원복
  🔴 **네트워크 정책을 열어야 Phase 5·6·8·10 이 풀린다** (MASTER_PLAN 4-0절)
- **직전 완료**: 계획서·작업일지 체계 구축, 저장소 정리(main), 자동 일지 훅 작동
- **오늘의 목표 한 줄**: 원격 컨테이너에서 LLM API 호출이 실제로 되는 것까지 확인한다.

> ⚠️ **전제 수정 (2026-08-31)**: 작업 환경이 로컬 PC가 아니라 **핸드폰 + 원격 컨테이너**임이 확인됨.
> 어제 세운 로컬 기준 항목(가상환경, pip install, 로컬 실행)은 전부 폐기하고 아래로 대체함.
> LLM도 Claude → **Gemini(Google 키)** 로 전환 예정.

---

## P — Plan (오늘 할 것)

작게 쪼갭니다. 한 항목은 **30분~1시간 안에 끝나는 크기**로.

> 📱 = **핸드폰에서 직접** 해야 하는 것 (제가 대신 못 함)
> 🤖 = Claude Code가 컨테이너에서 처리하는 것

**A. 키 준비 (여기서 막히면 아무것도 못 나감)**
- [x] 📱 **P0-A1** 폰 브라우저로 `aistudio.google.com` 접속 → Gemini API 키 발급 ✅
- [x] 📱 **P0-A2** 발급받은 키를 **환경 설정의 환경변수** 로 등록 (`GEMINI_API_KEY`) ✅
      ⚠️ 채팅창에 붙여넣지 말 것 — 대화 기록에 영구히 남음

**B. 코드 전환 (제가 함)**
- [x] 🤖 **P0-B1** `requirements.txt` → `google-genai` 2.20.0 교체 ✅ 설치 검증 완료
- [x] 🤖 **P0-B2** `scripts/hello.py` Gemini 전환 ✅ 키 없을 때 안내 동작 확인
- [x] 🤖 **P0-B3** 컨테이너에서 실제 실행 → 응답 받음 ✅ **Phase 0 완료 (2026-09-03)**

**폐기된 항목** (로컬 PC 전제였음)
- ~~P0-1 로컬 가상환경~~ / ~~P0-2b 로컬 pip install~~ / ~~P0-5b 로컬 실행~~
- [x] **P0-4** `.env` git 차단 확인 ✅ (여전히 유효)
- [x] **P0-2a / P0-5a** `requirements.txt`·`hello.py` 작성 ✅ (Claude용 → Gemini용으로 개작 예정)

### 오늘 안 할 것 (중요)
욕심내면 반드시 아무것도 못 끝냅니다. 오늘은 아래 손대지 않습니다.
- 이미지 생성 API — Phase 3
- 인스타 API / 계정 세팅 — Phase 5
- n8n / Docker — Phase 6

---

## D — Do (진행 기록)

작업하면서 여기에 한 줄씩 남깁니다. 시각 + 무슨 일이 있었는지.

- `16:5x` — Phase 0 착수 전, 작업 기준부터 세움. 계획서 3종(MASTER_PLAN / TODAY / 작업일지 양식) 작성.
- `16:5x` — SessionStart 훅 + git commit 자동 일지 훅 구축, 3가지 케이스(생성/중복차단/무시) 검증 완료.
- `16:5x` — `.env` 가 git에 안 올라가는 것 확인 (**P0-4 선행 완료**).
- `16:5x` — Gemini 원안의 오류 2건 확인 및 계획서에 반영 (모델명 구버전 / 인스타 댓글 링크 클릭 불가).
- `--` — 기본 브랜치를 `main` 으로 정리. 자동생성 브랜치 삭제 완료. PR은 불필요하다고 판단(혼자 작업, 합칠 대상 없음).
- `--` — `requirements.txt` 작성. 가상환경에 실제 설치해서 검증 (anthropic 1.2.0 / pillow 12.3.0 / dotenv 1.2.3 / requests 2.34.2).
- `--` — `scripts/hello.py` 작성. 키 없음 / 예시값 그대로 / 형식 오류 3가지 경로가 전부 친절한 안내를 내고 exit 1 하는 것 확인.
- `--` — **실제 API 호출은 안 함.** 키가 유효한지는 로컬에서 님이 직접 확인해야 하는 부분(P0-5b).
- `--` — 자동 일지 훅이 그동안 한 번도 안 걸리고 있던 것 발견. 패턴 버그 수정 후 **자동 생성 확인**.
- `--` — 훅 무한 반복(일지 커밋이 또 일지를 만듦) 차단 규칙 추가 및 범위 확대. 4가지 경우 검증 완료.
- `--` — **작업 환경 전제가 틀렸음을 확인.** 로컬 PC가 아니라 핸드폰 + 원격 컨테이너.
  LLM도 Claude → Gemini(Google 키)로 전환하기로 함.
- `--` — 컨테이너 네트워크 확인: Gemini API·Anthropic API **도달 가능**,
  `aistudio.google.com`(키 발급 페이지)은 **차단** → 폰 브라우저로 발급받아야 함.
- `--` — MASTER_PLAN 에 4-4절(작업 환경 제약) 추가, Phase 0/6 완료조건 수정.
- `--` — **첫 콘티를 손으로 제작** (키워드: 겨울 온열매트, 6컷). API 미사용.
  결과물에서 `toon.json` 스키마 v1 을 뽑아내 `docs/TOON_SCHEMA.md` 로 문서화. **P1-1 완료.**
- `--` — **Phase 1 콘티 생성기 코드 완성** (P1-2·P1-3·P1-4). 프롬프트 + Gemini 구조화 출력 + 검증기.
  `selftest.py` 로 API 없이 16개 항목 검증 통과. 허위광고 필터의 한글 수사 구멍(`천원`, `두 배`) 발견해 보강.
- `--` — **프로세스 개편.** 작화를 사람이 맡기로 함 → 캐릭터 일관성 리스크 + 이미지 생성 비용이 동시에 소멸.
- `--` — 트렌드 수집 경로 조사. SNS API 8곳 접속 테스트 → **googleapis 제외 전부 프록시 차단**.
  인스타는 트렌드 API 자체가 없고, 토스는 인앱 전용이라 웹 경로가 없음. **쿠팡파트너스 API 가 정답**으로 결론.
- `--` — 제미나이가 준 쿠팡 Playwright 스크래핑 코드 검토. URL이 베스트가 아닌 홈페이지라
  **애초에 아무것도 안 나오는 코드**였음. 우회 방식 자체를 채택하지 않기로 함(파트너스 계정 정지 위험).
- `--` — **Phase 2 말풍선 합성기 완성.** OFL 한글 폰트 3종 확보, `compose.py`·`contact_sheet.py` 작성.
  6컷 전부 합성 성공. 하단 말풍선이 고지 문구를 덮는 버그를 회귀 테스트로 잡아 수정.
- `2026-09-03` — **키 등록됨. 첫 LLM 실호출 성공.** `hello.py` 응답 확인 → **Phase 0 완료.**
- `2026-09-03` — 모델 세대가 통째로 바뀐 것 확인. `gemini-2.5-flash` 는 **신규 사용자 차단**(404),
  서버가 `gemini-3.6-flash` 이후를 안내. 사용 가능 모델 55개를 목록으로 뽑아 확인.
- `2026-09-03` — `make_toon.py` 가 `gemini-3.8-flash`(최신) 에서 **503 재현성 실패**.
  평문/작은 스키마는 되고 큰 요청만 실패 → 스키마 조각별로 이분 탐색해서
  **모델 용량 문제**로 확정(3.5-flash·3.1-flash-lite 는 같은 요청 정상).
- `2026-09-03` — 진단 중 연속 호출로 **429(분당 제한)** 를 밟음. 무료 등급은 폭주하면 막힌다.
- `2026-09-03` — 기본 모델을 `gemini-3.5-flash` 로 내리고, **503/429 자동 재시도 + 모델 폴백**
  (`generate_with_fallback`) 추가. 한 모델이 붐벼도 작업이 멈추지 않는다.
- `2026-09-03` — **콘티 3건 실제 생성 성공** (겨울 온열매트 / 무선 핸디 청소기 / 저소음 가습기).
  `response_schema` 형식이 맞는 것까지 확인 → **P1-5 완료.** selftest 23/23 통과.
- `2026-09-03` — **원안 복원.** 계획서가 원안에서 흘러내린 4가지를 되돌림:
  ①구글시트/노션 트리거 ②텔레그램 승인 봇 ③LLM 분업(그록/GPT/Claude) ④그록 트렌드 소싱.
  **원안에서 바뀐 건 작화=사람 하나뿐**임을 계획서 요약에 명시.
- `2026-09-03` — **네트워크 정책 실측.** 그록·GPT·텔레그램·슬랙·쿠팡·인스타 전부
  `403 policy denial`. 도달하는 건 **Anthropic·Google 둘뿐.**
  버그가 아니라 **환경 생성 시 고른 설정** → 사용자가 열 수 있음. MASTER_PLAN 4-0절 신설.
- `2026-09-03` — **문서/코드 모순 발견.** `CLAUDE.md` 는 콘티 담당이 Claude 인데
  실제 코드는 Gemini. Phase 0 의 임시 조치가 굳은 것. 임시임을 명시하고 **Phase 11**(원복) 신설.
- `2026-09-03` — **Phase 9 소재 큐 완성.** `content/queue.json` 스키마 +
  `scripts/queue.py`(add/list/next/done/drop) + `docs/SOURCING.md`(그록 질문 템플릿).
  **채우는 주체가 사람이든 그록 API든 뒷단이 안 바뀌는 구조** → Phase 8 이 드롭인 교체가 됨.
- `2026-09-03` — 한글 표 정렬 버그 수정. `:<22` 가 글자 수로만 세서 한글 열이 어긋나던 것을
  동아시아 문자 폭(2칸) 계산으로 교체. selftest **32/32 통과**.

---

## C — Check (확인)

퇴근 전 3분만 씁니다.

- **된 것**: 계획서·작업일지 체계와 자동 훅, 저장소 정리(main), 첫 콘티 수작업 제작,
  `toon.json` 스키마 v1, **말풍선 합성기(Phase 2) 완성**, 콘티 생성기 코드 + 자체점검 23항목,
  트렌드 수집 경로 조사 및 결론, 프로세스 개편,
  **Gemini 실호출 성공(Phase 0 완료)**, **콘티 3건 실제 생성(P1-5 완료)**,
  503/429 자동 재시도 + 모델 폴백.
- **안 된 것**: 작화가 아직 없어서 **말풍선이 인물 얼굴을 가리는지 모름.**
  콘티 품질에 구멍 하나 — **컷5(solution) 대사가 `…………` 로 두 번 연속 나옴.**
  무언의 만족을 노린 연출이지만 반복되면 패턴이라 프롬프트를 손봐야 한다.
  ~~LLM 실호출 검증 0회~~ → ✅ 2026-09-03 해소.
- **막힌 지점 / 에러 메시지**:
  - `git push origin --delete` → `HTTP 403`. 세션 권한 밖. 사용자가 웹 UI로 직접 삭제.
  - `pip install` → `files.pythonhosted.org ... Read timed out`. `--timeout 120 --retries 5` 로 해결.
  - 자동 일지 훅이 조용히 아무것도 안 하고 있었음. 패턴 `*"git commit"*` 이
    `git -c user.name=... commit` 을 못 잡은 것.
  - `gemini-2.5-flash` → **404, 신규 사용자 차단.** 모델명은 계속 늙는다. 목록으로 확인할 것.
  - `gemini-3.8-flash` → **503 재현성 실패.** 최신 모델이라고 무료 등급에서 쓸 수 있는 게 아니다.
  - 연속 호출 6회 → **429.** 진단할 때도 간격을 둘 것.
  - **네트워크 정책이 거의 모든 외부 API를 막고 있음.** 쿠팡·네이버·토스·X·레딧·틱톡·
    `graph.facebook.com` 전부 `HTTP 000`. 도달하는 건 `googleapis.com` 하나.
    → 트렌드 수집뿐 아니라 **인스타 업로드 연동도 여기서는 못 돌린다.**
- **예상보다 오래 걸린 것**: 훅 디버깅(버그 3개 연달아), 그리고 **전제 확인 없이 진행한 대가**.
  작업 환경(로컬 PC→핸드폰)과 LLM(Claude→Gemini)을 두 번 다 뒤엎었다.

---

## A — Act (다음)

- **내일 첫 번째로 할 일**: 사용자가 신청/발급을 끝내고 오면 그때 이어간다.
  그 전에라도 **상품명 하나만 주면 콘티는 바로 뽑을 수 있다.**
- **계획 수정할 것**: ✅ 완료 — MASTER_PLAN 에 역할 분담 개편, 소재 수집 조사 결과(4-5절),
  Phase 3 재정의(작화=사람), Phase 8(소재 자동화) 추가 반영함.
- **일지 남겼는가**: [x] 일지 #01~#09

### 사용자가 해야 할 일 (코드로 더 못 나가는 지점)

**🔴 최우선 — 네트워크 정책 열기**
- [ ] claude.ai/code → 환경 설정 → **네트워크 정책**에서 아래 도메인 허용
      `api.x.ai`(그록) / `api.openai.com` / `api.telegram.org` /
      `api-gateway.coupang.com` / `graph.facebook.com` / `openapi.naver.com`
      → **Phase 5·6·8·10 이 한 번에 풀린다.** 지금 가장 값어치 큰 단일 조치

**승인 대기가 있어 지금 신청해야 하는 것**
- [ ] 쿠팡파트너스 가입 신청 — 링크 생성과 순위 API 둘 다 여기 걸림
- [ ] 인스타 프로페셔널 계정 전환 + 페이스북 페이지 연결 — Graph API 전제조건

**바로 되는 것**
- [x] Gemini API 키 발급 → 환경변수 등록 ✅ 2026-09-03
- [ ] 폰 그록 앱에 트렌드 질문 던져보기 (템플릿: `docs/SOURCING.md`) — **공짜 검증**
- [ ] 앤트로픽 키 발급 → 콘티를 원안대로 Claude 로 되돌리기 (Phase 11)

**나중 (Phase 5 전까지)**
- [ ] 링크인바이오 페이지 — 없으면 클릭이 0이다
- [ ] 이미지 호스팅 경로 — Graph API 는 로컬 jpg 를 안 받고 공개 URL 을 요구함

---

<details>
<summary>다음 Phase 미리보기 (Phase 1 — 콘티 생성기)</summary>

- [x] P1-1 `toon.json` 스키마 확정 ✅ (손으로 만든 1호에서 역으로 뽑아냄)
- [x] P1-2 콘티 생성 프롬프트 작성 ✅ `prompts/toon_prompt.md`
- [x] P1-3 `scripts/make_toon.py` 작성 ✅ (Gemini. 실호출 검증은 키 필요)
- [x] P1-4 구조화 출력(response_schema) 적용 ✅
- [x] P1-5 키워드 3개로 테스트, 결과 품질 눈으로 확인 ✅ (온열매트/핸디청소기/가습기)

</details>
